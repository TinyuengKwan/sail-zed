(fun_def
  "function" @context
  .
  (funcls
    (id) @name)) @item

(val_spec_def
  "val" @context
  (id) @name) @item

(type_def
  "type" @context
  (id) @name) @item

(type_def
  "struct" @context
  (id) @name) @item

(type_def
  "enum" @context
  (id) @name) @item

(type_def
  "union" @context
  (id) @name) @item

(type_def
  "bitfield" @context
  (id) @name) @item

(map_def
  "mapping" @context
  (id) @name) @item

(register_def
  "register" @context
  (id) @name) @item
